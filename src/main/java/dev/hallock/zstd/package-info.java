/**
 * Zstd bindings for Java. These are high level bindings to the native ZSTD library, and provide a
 * more user friendly interface for most users. For low level bindings, see {@link
 * dev.hallock.zstd.bindings}.
 */
@NullMarked
package dev.hallock.zstd;

import org.jspecify.annotations.NullMarked;
